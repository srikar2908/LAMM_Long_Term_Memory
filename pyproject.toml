[project]
name = "lamm-long-term-memory"
version = "0.1.0"
description = "Lightweight Adaptive Memory Management for Long-Term LLM Agents"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
